spring:
  application:
    name: krishi-mitra

  datasource:
    url: jdbc:postgresql://localhost:5432/krishi_mitra_db
    username: postgres
    password: sanu
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

app:
  jwt:
    secret: KrishiMitraSecretKey2024SuperLongSecretKeyForJWTAuthentication
    expiration: 86400000

weather:
  api:
    key: YOUR_OPENWEATHER_API_KEY
    url: https://api.openweathermap.org/data/2.5/weather